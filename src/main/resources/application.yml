server:
  port: 8080

spring:
  application:
    name: task-manager
  profiles:
    active: dev   # default local
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 1